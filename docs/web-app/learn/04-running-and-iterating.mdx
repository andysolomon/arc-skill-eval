Run results are useful when they point to a concrete prompt gap, weak assertion, missing fixture, or skill behavior that needs tightening.

<Note>Read the grading summary first, then compare the answer and trace before changing the suite.</Note>

```sh
arc-skill-eval run evals/evals.json --compare
```

format · Anthropic evals.json / method · OpenAI eval-skills / runtime · Pi
