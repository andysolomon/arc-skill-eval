A skill eval is a focused check that asks whether a skill changes observable agent behavior in a promised direction.

<Note>Good evals measure produced answers, files, tool use, or traces instead of checking whether the model claims it used a skill.</Note>

```ts
const evidence = ["assistant output", "workspace artifact", "trace event"];
```

format · Anthropic evals.json / method · OpenAI eval-skills / runtime · Pi
