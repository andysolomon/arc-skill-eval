A positive skill delta is only part of the decision; authors should also weigh runtime cost, latency, maintenance, and how often the task occurs.

<Note tone="warning">A suite that wins by making every case bigger may be less useful than a smaller suite with a clear signal.</Note>

```ts
const value = skillLift - (tokenCost + maintenanceCost);
```

format · Anthropic evals.json / method · OpenAI eval-skills / runtime · Pi
