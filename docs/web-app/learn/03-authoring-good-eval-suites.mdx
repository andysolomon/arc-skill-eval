An eval suite should start from one manual example, capture the behavior contract, and grow only after the first case produces inspectable evidence.

<Note>Case names should describe scenarios, not implementation details.</Note>

```json
{ "id": "writes-actionable-pr-review", "assertions": ["names blocking risk"] }
```

format · Anthropic evals.json / method · OpenAI eval-skills / runtime · Pi
