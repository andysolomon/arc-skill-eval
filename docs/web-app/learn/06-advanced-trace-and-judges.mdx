Advanced review starts when the shaped surfaces do not explain a failure and the trace or judge prompt needs direct inspection.

<Note>Use trace evidence to connect runtime behavior back to the assertion that passed or failed.</Note>

```json
{ "trace": "tool-call-summary", "judge": "evidence-focused" }
```

format · Anthropic evals.json / method · OpenAI eval-skills / runtime · Pi
