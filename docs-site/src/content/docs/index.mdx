---
title: Skeval
description: Run Anthropic-format skill evals, grade them with LLM and script assertions, and compare them with a no-skill baseline.
template: splash
hero:
  tagline: Run and compare skill evals in Anthropic's published format.
  actions:
    - text: Get started
      link: /arc-skill-eval/quickstart/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/andysolomon/arc-skill-eval
      icon: external
      variant: minimal
---

## What is Skeval?

**Skeval** documents [`arc-skill-eval`](https://github.com/andysolomon/arc-skill-eval), a Pi-native CLI for running skill evals. Put `evals/evals.json` next to `SKILL.md`. The CLI finds the suite, runs each case with the skill attached, grades the output, and writes artifacts you can inspect or compare.

The grading workflow comes from OpenAI's [Testing Agent Skills Systematically with Evals](https://developers.openai.com/blog/eval-skills): start with a small suite, add cases from real failures, and compare the same prompts with and without the skill. Files follow [Anthropic's published format](https://platform.claude.com/docs/en/agents-and-tools/agent-skills), including `evals/evals.json`, `grading.json`, and `benchmark.json`. The runtime design draws on [Ampcode's How to Build an Agent](https://ampcode.com/notes/how-to-build-an-agent) and Mihail Eric's [The Emperor Has No Clothes](https://www.mihaileric.com/The-Emperor-Has-No-Clothes/). [Inspiration and credits](/arc-skill-eval/inspiration/) has the full attribution.

The brand is Skeval. The package and the CLI binary are still `arc-skill-eval`.

## Why it exists

Skills are markdown files with optional scripts. They can change model behavior without making the change easy to measure. Skeval runs the same prompt with and without the skill, then grades both runs against the same assertions. The pass-rate difference shows whether the skill helped.

## What you get

- `evals/evals.json` as the authoring format, using Anthropic's published shape.
- LLM-judged assertions for prose claims, deterministic script assertions for file presence, regex, and JSON validity.
- Opt-in `with_skill` vs `without_skill` dual runs and a `benchmark.json` aggregate.
- Per-case observability: `assistant.md`, `outputs/`, `timing.json`, `trace.json`, `tool-summary.json`, `context-manifest.json`.
- Model pinning for runner and judge models, including GPT 5.5 and Ollama Cloud through Pi.
- An authoring skill, [`arc-creating-evals`](/arc-skill-eval/authoring-evals/), that turns success criteria into cases, assertions, and fixtures.
- A public roadmap for runtime setup, review reports, feedback-driven changes, and description testing.

## Where to start

- **[Quickstart](/arc-skill-eval/quickstart/).** Install the CLI, run the `hello-world` skill, and read `grading.json`.
- **[Concepts](/arc-skill-eval/concepts/).** Learn about skills, cases, assertions, grading, artifacts, and the with/without difference.
- **[Authoring evals](/arc-skill-eval/authoring-evals/).** Write `evals/evals.json` for your own skill.
- **[Dogfooding and authoring loop](/arc-skill-eval/dogfooding/).** Use `--compare`, iterations, and artifacts to improve a skill.
- **[Runtime and models](/arc-skill-eval/runtime-and-models/).** Configure Pi defaults, pinned models, GPT 5.5, Ollama Cloud, and eval-owned runtimes.
- **[Skill creator roadmap](/arc-skill-eval/skill-creator-roadmap/).** See planned commands and product work.
- **[Blog](/arc-skill-eval/blog/).** Read why skill evals matter and what I learned while building this project.
- **[Inspiration and credits](/arc-skill-eval/inspiration/).** Read the sources that shaped the project.
